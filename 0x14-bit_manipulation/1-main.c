#include "main.h"
#include <limits.h>
/**
 * main - The entry point of the program
 *
 * Description: Uses print_binary function to print the binary representation
 * of a decimal number
 * Return: Always 0 (Success)
 */
int main(void)
{
	print_binary(ULONG_MAX);
	return (0);
}
