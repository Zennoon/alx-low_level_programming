#include "main.h"

/**
 * main - The entry point of the program
 *
 * Description: Uses the _print_rev_recursion function to print a string in
 * reverse recursively
 * Return: Always 0 (Success)
 */
int main(void)
{
	_print_rev_recursion("\nColton Walker");
	return (0);
}
