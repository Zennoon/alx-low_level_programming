#include "main.h"

/**
 * main - The entry point of the program
 *
 * Description: Prints a string using _puts_recursion function which works
 * recursively, printing one character, then passing the rest of the string
 * to the function
 * Return: Always 0 (Success)
 */
int main(void)
{
	_puts_recursion("Puts with recursion");
	return (0);
}
