#include <stdio.h>
#include "4-sum.h"

/**
 * main - The entry point of the program
 *
 * Description: Uses the function like macro SUM defined in 4-sum.h to calc
 * the sum of two numbers
 * Return: Always 0 (Success)
 */
int main(void)
{
	printf("%d\n", SUM(98, 1024));
	return (0);
}
