#include "main.h"

/**
 * main - The entry point of the program
 *
 * Description: Prints a line of underscores ('_') of specified size
 * Return: Always 0 (Success)
 */
int main(void)
{
	print_line(10);
	print_line(0);
	print_line(-5);
	print_line(5);
	return (0);
}
