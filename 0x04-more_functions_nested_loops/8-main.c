#include "main.h"

/**
 * main - The entry point of the program
 *
 * Description: Prints a square made of '#' of a given size
 * Return: Always 0  (Success)
 */
int main(void)
{
	print_square(10);
	print_square(0);
	print_square(-4);
	print_square(5);
	return (0);
}
