#include "main.h"

/**
 * main - The entry point of the program
 *
 * Description: Prints numbers 0 - 14 as characters followed by a new line
 * 10 times using more_numbers function
 * Return: Always 0 (Success)
 */
int main(void)
{
	more_numbers();
	return (0);
}
