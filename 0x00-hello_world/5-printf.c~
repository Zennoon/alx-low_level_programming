#include <stdio.h>

/**
 * main - the entry point for this program
 *
 * Description: Inside the function, printf is used to print a string literal
 * to standard output
 * Return: Always 0 (Success)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}
