#include "main.h"

/**
 * main - The entry point of the program
 *
 * Description: The function uses print_alphabet_10x which in turn uses _putchar.
 * Return: Always 0 (Success)
 */
int main(void)
{
	print_alphabet_10x();
	return (0);
}
