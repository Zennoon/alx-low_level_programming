#include "main.h"

/**
 * main - The entry point of the program
 *
 * Description: The function prints a times table of given size using
 * print_times_table
 * Return: Always 0 (Success)
 */
int main(void)
{
	print_times_table(15);
	print_times_table(3);
	print_times_table(6);
	return (0);
}
