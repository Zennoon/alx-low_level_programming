#include "main.h"

/**
 * main - The entry point of the function
 *
 * Description: The function uses _islower function to check whether some
 * character is lowercase or not
 * Return: Always 0 (Success)
 */
int main(void)
{
	int r;

	r = _islower('H');
	_putchar(r);
	return (0);
}
