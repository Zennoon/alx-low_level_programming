#include <stdio.h>

/**
 * main - The entry point of the program???
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	printf("So, there are ways to make functions execute before main\n");
	return (0);
}
