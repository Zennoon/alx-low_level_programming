#include <stdio.h>
#include "variadic_functions.h"

/**
 * main - The entry point of the program
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	print_all("ceisf", 'B', 3, "stSchool", 2.345);
	return (0);
}
